# Skill Registry — hexarena

Generated by `sdd-init`. Project is greenfield (no code yet); registry lists available user-level skills relevant to future delegation.

## SDD Phase Skills
- **sdd-init** (`~/.claude/skills/sdd-init/SKILL.md`) — bootstraps SDD context/persistence. Delegate-only, executor-side.
- **sdd-explore** (`~/.claude/skills/sdd-explore/SKILL.md`) — investigates ideas/codebase before proposal. No files created.
- **sdd-propose** (`~/.claude/skills/sdd-propose/SKILL.md`) — writes proposal (intent/scope/approach).
- **sdd-spec** (`~/.claude/skills/sdd-spec/SKILL.md`) — writes delta specs (requirements + scenarios).
- **sdd-design** (`~/.claude/skills/sdd-design/SKILL.md`) — architecture/design doc, requires proposal.
- **sdd-tasks** (`~/.claude/skills/sdd-tasks/SKILL.md`) — breaks down into task checklist, requires spec+design.
- **sdd-apply** (`~/.claude/skills/sdd-apply/SKILL.md`) — implements tasks, marks checklist as it goes.
- **sdd-verify** (`~/.claude/skills/sdd-verify/SKILL.md`) — validates implementation vs spec/design/tasks.
- **sdd-archive** (`~/.claude/skills/sdd-archive/SKILL.md`) — merges delta specs, closes change.
- **sdd-onboard** (`~/.claude/skills/sdd-onboard/SKILL.md`) — guided end-to-end SDD walkthrough.

## Domain Skill
- **celopedia-skill** (`~/.claude/skills/celopedia-skill/SKILL.md`) — Celo/MiniPay ecosystem expert: fee abstraction (CIP-64), MiniPay copy rules (no "gas"/"crypto"), contract addresses, builder guide, DeFi protocols. **Directly relevant**: Arena deposit flow (USDC/USDT adapters), MiniPay Mini App scaffolding, submission requirements.

## Delivery / Git Workflow Skills
- **branch-pr** — Gentle AI PR creation with issue-first checks.
- **issue-creation** — Gentle AI issue creation with issue-first checks.
- **chained-pr** — split oversized changes (>400 lines) into chained PRs.
- **work-unit-commits** — plan commits as reviewable work units.
- **code-review** — review diff for correctness/simplification at configurable effort.
- **simplify** — reuse/simplification/efficiency cleanup pass.
- **judgment-day** — blind dual review + fix + re-judge.

## Studio / Scaffold Skills
- **scaffold** — builds a new project from scratch in an empty folder (vision → stack → MVP → base structure). Overlaps with this SDD flow; SDD chosen per user request.
- **studio-init** / **studio-director** — alternative agent-build pipeline (brief → build → verify); not used here per user's explicit SDD request.
- **skill-creator** — create new LLM-first skills.
- **skill-registry** — regenerate this registry after skill changes.

## Compact Rules (cross-cutting)
- MiniPay UI copy: never show "gas", "crypto", raw `0x...` addresses to users — use "network fee", "stablecoin", phone/alias.
- MiniPay token scope: only USDT/USDC/USDm; never surface CELO.
- Fee abstraction: use adapter addresses for USDC/USDT, token addresses for USDm/EURm/BRLm.
- PRs >400 lines → use chained-pr / work-unit-commits before requesting review.
- Never invent contract addresses — verify against `contracts.md` / docs.celo.org.
